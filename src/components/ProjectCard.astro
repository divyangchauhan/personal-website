---
// ProjectCard — single project card with impact metrics and expandable detail

interface Props {
  title: string;
  role: string;
  period: string;
  description: string;
  liveUrl?: string;
}

const { title, role, period, description, liveUrl } = Astro.props;
---

<div class="project-card">
  <!-- ProjectCard skeleton: {title} -->
</div>

<style>
</style>
